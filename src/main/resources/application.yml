spring:
  application:
    name: museum
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/museum}
    username: ${DB_USERNAME:museum}
    password: ${DB_PASSWORD:museum}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  flyway:
    enabled: true

  mail:
    host: ${MAIL_HOST:mail.kononovmuseum.ru}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:no-reply@kononovmuseum.ru}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: ${MAIL_SMTP_AUTH:true}
          starttls:
            enable: ${MAIL_SMTP_STARTTLS:true}
            required: false
          ssl:
            checkserveridentity: false
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  port: ${PORT:8080}

logging:
  level:
    org.hibernate.SQL: warn
    org.springframework.security: info

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

app:
  notifications:
    from: ${NOTIFY_FROM_EMAIL:no-reply@kononovmuseum.ru}
